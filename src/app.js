const express = require('express');
const app = express();

// Import routes

// Settings

// Middleware

// Starting Server
app.listen(3000, () => {
   console.log('Servidor Escuchando en Puerto 3000')
});